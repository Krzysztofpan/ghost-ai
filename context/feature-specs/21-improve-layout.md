Canvas should be big as posible on the all window with, sidebar and ai toolbar should be shown over the canvas
as windows.

## What to do

- resize the canvas to be on whole screen
- change sidebar and ai toolbar to be like popups or windows displaying over canvas, but not reisizing canvas
- don't chagne the logic in the sidebar or ai toolbar
- don't change the logic in canva only resize canva

## Check if done

- sidebar and ai toolbar when are open showing over the canva
- canvas on the whole avaliable width
- user can without problem open and close sidebar and ai toolbar
- `npm run build` passes